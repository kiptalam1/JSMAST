import 'dotenv/config';
import { Client } from 'pg';

const client = new Client({
  host: 'ep-withered-cell-atcht2aq-pooler.c-9.us-east-1.aws.neon.tech',
  port: 5432,
  user: 'neondb_owner',
  password: 'npg_HMGlETao9m5Q'
  database: 'jsmast',
  ssl: {
    rejectUnauthorized: false,
  },
});

try {
  await client.connect();
  console.log('Connected!');
} catch (e) {
  console.error(e);
}
